(fs) {
	file_server {
		browse {$BROWSETEMPLATE}
		root {args[0]}
		precompressed
	}
}

(fileserver) {
	redir {args[0]} {args[0]}/ 301
	handle_path {args[0]}/* {
		{blocks.pre}
		import fs {args[1]}
	}
}
