import hostmatch rtc
handle @rtc {
	route {
		@live `!path('*/whep*') && !method('GET', 'HOST', 'OPTIONS')`
		respond @live "writer no writing!" 405
		import auth
		reverse_proxy localhost:8889
	}
}

import hostmatch hls
handle @hls {
	route {
		import auth
		reverse_proxy localhost:8888
	}
}
